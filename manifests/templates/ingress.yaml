---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recipes                  # Ingress name
  labels:
    app: recipes
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "recipes.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: recipes               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
